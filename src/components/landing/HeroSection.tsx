import HeroText from "./HeroText"

const HeroSection = () => {
  return (
    <div className="flex flex-col gap-2 max-sm:pt-12 sm:pt-8">
        <HeroText />
    </div>
  )
}

export default HeroSection